version: "3"
services:
  actual_server:
    image: actualbudget/actual-server:23.12.1
    container_name: actual_server
    volumes:
      - ./data:/data
    restart: 'unless-stopped'
    networks:
      - frontend 

networks:
  frontend:
    name: frontend
    external: true
